# Rust build output
/target/

# Cargo.lock is not committed for library crates (cdylib).
# If you are vendoring or pinning for CI, remove this line.
Cargo.lock

# Task runner state (checksums for up-to-date detection)
.task/

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Built wheels and distribution archives
/dist/

# Compiled protobuf descriptors (generated by `task demo:descriptor`)
demo/people/descriptors/

# Django dev database
demo/db.sqlite3

# Pre-compiled native libraries (populated by build:centos7 / build:linux-gnu, not committed)
django_sqlite_protobuf/django_sqlite_protobuf/libs/

# Virtual environments
.venv/
