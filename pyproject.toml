[project]
name = "sqlite-protobuf"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "protobuf>=7.34.0",
    "pytest>=8.0",
    "django>=4.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
