[project]
name = "sqlite-protobuf-demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "django>=4.2",
    "protobuf>=5.0",
    "django-sqlite-protobuf",
    "django-tables2>=2.7",
]

[tool.uv.sources]
django-sqlite-protobuf = { path = "../django_sqlite_protobuf", editable = true }
